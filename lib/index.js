/**
 * @local/usage-trends — Host half (no-op).
 * The client half (lib/client.js) carries the trends view tab;
 * this host module only keeps the preset row valid in the loader.
 */
export const name = '@local/usage-trends'
export function apply() {}
